version: '3.8'

services:
  blocking_server:
    build:
      context: .
    ports:
      - "8081:8080"
    command: ./blocking_server

  nonblocking_server:
    build:
      context: .
    ports:
      - "8082:8080"
    command: ./nonblocking_server
